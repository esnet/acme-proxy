---
- name: reload systemd daemon
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart acme-proxy
  become: true
  ansible.builtin.systemd:
    name: acme-proxy
    state: restarted
